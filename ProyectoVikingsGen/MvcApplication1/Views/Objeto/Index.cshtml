@model IEnumerable<ProyectoVikingsGenNHibernate.EN.ProyectoVikings.ObjetoEN>

@{
    ViewBag.Title = "Objetos";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Objetos</h2>

<p>
    @Html.ActionLink("Nuevo Objeto", "Create")
</p>
<p>
    @Html.ActionLink("Mis Objetos", "List")
</p>
<table>
    <tr>
        
        <th>
            @Html.DisplayNameFor(model => model.Equipo.Nombre)
        </th>

    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Equipo.Nombre)
            </td>
            
            <td>
                @Html.ActionLink("Editar", "Edit", new { id = item.Id }) |
                @Html.ActionLink("Detalles", "Details", new { id = item.Id }) |
                @Html.ActionLink("Borrar", "Delete", new { id = item.Id }) |
                @Html.ActionLink("Ver Equipo", "Details", "Equipo", new { id = item.Id }, "null") |
                @Html.ActionLink("Ver Inventario", "Details", "Inventario", new { id = item.Id }, "null")

            </td>

        </tr>
    }

</table>